<UserControl x:Class="NuGet.PackageManagement.UI.OpenReadMeMarkdownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             xmlns:local="clr-namespace:NuGet.PackageManagement.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <FrameworkElement.CommandBindings>
      <CommandBinding Command="{x:Static markdig:Commands.Hyperlink}" Executed="OpenHyperlink" />
      <CommandBinding Command="{x:Static markdig:Commands.Image}" Executed="ClickOnImage" />
  </FrameworkElement.CommandBindings>
    <DockPanel>
        <markdig:MarkdownViewer x:Name="Viewer" Markdown="{Binding FileContent}"/>
  </DockPanel>

</UserControl>
