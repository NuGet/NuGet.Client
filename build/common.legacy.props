<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0">
  <PropertyGroup>
    <_WasCommonPropsImported>true</_WasCommonPropsImported>
    <IsNetCoreProject>false</IsNetCoreProject>
  </PropertyGroup>
  
  <!-- Load config -->
  <Import Project="config.props" />

  <!-- Additional props -->
  <Import Project="common.project.props"/>

  <PropertyGroup>
    <!-- Some legacy projects reference assemblies in GAC;AssemblyFolders. -->
    <!-- Reset AssemblySearchPaths to empty to let Microsoft.Common.targets pick the legacy defaults -->
    <AssemblySearchPaths />
  </PropertyGroup>
</Project>
